#!/usr/bin/env zsh
# vim: set ft=zsh:
# =============================================================================
# aliases/node.zsh — ros2 node shortcuts
# Prefix: rn (ros2 node)
# =============================================================================

alias rnl='ros2 node list'                     # List all nodes
alias rni='ros2 node info'                     # Node info (needs arg)
