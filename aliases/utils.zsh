#!/usr/bin/env zsh
# vim: set ft=zsh:
# =============================================================================
# aliases/utils.zsh — utility aliases
# Prefix: ru (ros2 utils)
# =============================================================================

alias rdoc='ros2 doctor'                       # Run ROS2 doctor
alias rdoce='ros2 doctor -r'                   # Run ROS2 doctor with report
alias rver='ros2 --version'                    # Show ROS2 CLI version
