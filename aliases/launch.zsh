#!/usr/bin/env zsh
# vim: set ft=zsh:
# =============================================================================
# aliases/launch.zsh — ros2 launch shortcuts
# Prefix: rl (ros2 launch)
# =============================================================================

alias rl='ros2 launch'                         # Launch a file (needs args)
